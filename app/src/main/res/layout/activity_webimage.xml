<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@color/black">
    <com.fujia.hanglife.widget.ZoomableImageView
        android:id="@+id/webimageIV"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:scaleType="matrix"
        android:src="@mipmap/bg_default"/>

    <TextView
        android:id="@+id/webimage_saveTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:padding="10dp"
        android:drawableLeft="@mipmap/ic_download"
        android:text="@string/webimage_saveTV"
        android:textColor="@color/bg_bottom"/>
</RelativeLayout>